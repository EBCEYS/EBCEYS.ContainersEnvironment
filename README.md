# EBCEYS.ContainersEnvironment

## ќписание

Ѕиблиотека дл€ удобной работы сервисов внутри docker контейнера.

## ѕеременные окружени€

`public class ServiceEnvironmentVariable<T>` используетс€ дл€ доступа к переменным окружени€.

`public static class DefaultEnvironmentVariables` содержит внутри себ€ инстансы переменных окружени€ общих дл€ контейнеров.

`public interface IEnvironmentConverter` интерфейс описывающий методы дл€ конвертации переменной окружени€ в нужный тип.

## »зменени€

### v0.0.1:

1. –абота с переменныим окружени€.